<p id="notice"><%= notice %></p>

<%= image_tag @image.image_attr.url(:medium) %>


<%= link_to 'Edit', edit_image_path(@image) %> |
<%= link_to 'Back', images_path %>

<%= @image.rating_score%>

<%= render "rating" %>